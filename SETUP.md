# راهنمای سیستم مدیریت نامه‌نگاری اداری

## معرفی
این یک سیستم جامع مدیریت نامه‌نگاری اداری با امکانات پیشرفته است که شامل:

- **ویرایشگر نامه شبیه Microsoft Word** با تمام تنظیمات فونت و پاراگراف
- **سیستم جریان کاری تایید** (Approval Workflow)
- **مدیریت سازمانی سلسله‌مراتبی**
- **کارتابل مرکزی** برای دریافت و ارجاع نامه‌ها
- **بخش مدیریت ادمین** برای کنترل سیستم

## نحوه استفاده

### 1. نصب و راه‌اندازی
```bash
# نصب dependencies
npm install

# اجرای dev server
npm run dev
```

سیستم خودکار به تنظیم دیتابیس اقدام می‌کند.

### 2. حساب‌های نمونه

سیستم با سه حساب کاربری نمونه فراهم شده است:

| نام | نام‌کاربری | رمز | نقش |
|------|-----------|-----|------|
| مدیر سیستم | admin | admin123 | ادمین (دسترسی کامل) |
| مدیر HR | manager | user123 | مسئول بخش (تأیید نامه‌ها) |
| کارمند | user | user123 | کارمند عادی |

### 3. صفحات اصلی

#### صفحه ورود
- آدرس: `/login`
- وارد کردن نام‌کاربری و رمز عبور

#### داشبورد
- آدرس: `/dashboard`
- نقطه شروع پس از ورود

#### نامه‌نگاری
- آدرس: `/dashboard/letter-writing`
- **ویژگی‌های ویرایشگر:**
  - تغییر فونت (Arial, Times New Roman, Georgia, etc.)
  - تغییر سایز فونت (8 تا 32)
  - تغییر فاصله سطر (1 تا 2)
  - **قالب‌دهی متن:** بولد، ایتالیک، زیرخط
  - **رنگ متن و هایلایت**
  - **تراز**: راست، وسط، چپ
  - **عنوان‌ها:** H1، H2
  - **لیست‌ها:** نقطه‌ای و شماره‌دار

#### کارتابل
- آدرس: `/dashboard/inbox`
- دریافت نامه‌هایی که برای تأیید ارسال شده‌اند
- تأیید یا رد نامه‌ها با اضافه کردن نظر

#### پنل مدیریت
- آدرس: `/dashboard/admin`
- **دسترسی محدود به ادمین‌ها**

## جریان کار نامه‌نگاری

### 1. **نوشتن نامه**
- کاربر به قسمت نامه‌نگاری می‌رود
- موضوع، گیرنده و متن نامه را وارد می‌کند
- می‌تواند پیش‌نویس ذخیره کند یا برای تأیید ارسال کند

### 2. **تأیید نامه**
- مسئول بخش گیرنده در کارتابل نامه را می‌بیند
- می‌تواند نامه را تأیید یا رد کند
- می‌تواند نظر یا توضیح اضافه کند

### 3. **ارسال نامه**
- پس از تأیید، نامه به وضعیت "sent" می‌رسد
- مسئول می‌تواند نامه را به زیردستان خود ارجاع دهد

### 4. **مدیریت کارتابل**
- هر کاربر کارتابل شخصی دارد
- نامه‌های دریافتی را مدیریت می‌کند
- می‌تواند نامه را نشان‌شده/ارجاع شده علامت‌گذاری کند

## ساختار دیتابیس

### جدول‌های اصلی:

1. **users** - کاربران سیستم
2. **departments** - بخش‌های سازمانی (سلسله‌مراتبی)
3. **letters** - نامه‌های سیستم
4. **inbox** - کارتابل کاربران
5. **approvals** - درخواست‌های تأیید
6. **letter_formatting** - تنظیمات فرمت‌دهی نامه

## تکنولوژی‌های استفاده‌شده

- **Frontend:** React, Next.js 16, TypeScript
- **UI Components:** shadcn/ui
- **Text Editor:** TipTap (Prosemirror)
- **Database:** SQLite 3
- **Authentication:** Custom with bcryptjs
- **Styling:** Tailwind CSS

## امکانات پیشرفته

### 1. ویرایشگر پیشرفته
- پیش‌نمایش WYSIWYG
- حفظ فرمت‌دهی هنگام ارسال
- تنظیمات کامل فونت و پاراگراف

### 2. سیستم تأیید
- جریان کار طبقه‌بندی‌شده
- امکان رد کردن با نظر
- پیگیری وضعیت نامه

### 3. مدیریت سازمانی
- ساختار سلسله‌مراتبی
- اختصاص مسئول برای هر بخش
- سطح‌های دسترسی مختلف

## نکات مهم

1. **رمزگذاری**: تمام رمزهای عبور با bcryptjs رمزگذاری می‌شوند
2. **Session**: جلسات در cookies ذخیره می‌شوند (اختیار 1 هفته)
3. **دیتابیس**: اول اجرا دیتابیس خودکار ایجاد می‌شود
4. **جهت متن**: سیستم برای زبان فارسی بهینه‌سازی شده است (RTL)

## مشکلات و حل‌آن

### اگر دیتابیس مشکل داشت:
```bash
# حذف دیتابیس
rm -rf data/

# اجرای دوباره
npm run dev
```

### اگر دسترسی مشکل داشت:
- مطمئن شوید که ورود صحیح انجام شده است
- دوباره وارد سیستم شوید

## درخواست‌های آینده

برای اضافه کردن خصوصیات جدید، می‌توانید:
1. کاربران جدید افزوید
2. بخش‌های جدید ایجاد کنید
3. الگوهای نامه تعریف کنید
4. گزارش‌های سفارشی ایجاد کنید
